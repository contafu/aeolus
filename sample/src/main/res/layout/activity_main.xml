<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@id/button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Request"
        android:textAllCaps="false"
        app:layout_constraintBottom_toTopOf="@id/next"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias=".5"
        app:layout_constraintVertical_chainStyle="packed" />

    <Button
        android:id="@+id/next"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="UploadFile"
        android:textAllCaps="false"
        app:layout_constraintBottom_toTopOf="@id/coroutine"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/button" />

    <Button
        android:id="@+id/coroutine"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Coroutine"
        android:textAllCaps="false"
        app:layout_constraintBottom_toTopOf="@id/thread_pool"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/next" />

    <Button
        android:id="@+id/thread_pool"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="ThreadPool"
        android:textAllCaps="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/coroutine" />

</androidx.constraintlayout.widget.ConstraintLayout>